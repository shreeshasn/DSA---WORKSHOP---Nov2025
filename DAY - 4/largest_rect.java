public class largest_rect {
    public static void main(String[] args) {
        int[] arr = { 1, 2, 3, 4, 6, 5, 3};
        int[] left = new int[arr.length];
        int[] right = new int[arr.length];

        left[arr.length] = -1;
        right[0] = arr.length; // keep arr length as max

        int r=arr.length-2;
        int l=1;

        while( r>=0 && l<arr.length-1)
        {
            for(int i=l; i<arr.length-1; i++)
            {
                
                
            }
            for(int r)
        }
    }
}
